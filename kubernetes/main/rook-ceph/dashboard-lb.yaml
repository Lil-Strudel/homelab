apiVersion: v1
kind: Service
metadata:
  name: rook-ceph-mgr-dashboard
  namespace: rook-ceph
spec:
  type: LoadBalancer
  selector:
    app: rook-ceph-mgr
  ports:
    - protocol: TCP
      port: 8443
      targetPort: 8443
