apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# User-facing workloads for the `main` cluster. Reconciled last, after the
# platform is up (see clusters/main/apps.yaml → apps, dependsOn infra-configs).
# No workloads are deployed yet — add app directories here as they arrive.
resources: []
