apiVersion: operator.victoriametrics.com/v1beta1
kind: VMPodScrape
metadata:
  name: shlink-pg
  namespace: shlink
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: shlink-pg
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 30s
