apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shlink-data
  namespace: shlink
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
