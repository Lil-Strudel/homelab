apiVersion: operator.victoriametrics.com/v1beta1
kind: VMPodScrape
metadata:
  name: vaultwarden-pg
  namespace: vaultwarden
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: vaultwarden-pg
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 30s
