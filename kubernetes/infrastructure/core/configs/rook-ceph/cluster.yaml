apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ceph-cluster
  namespace: rook-ceph
spec:
  interval: 1h
  chart:
    spec:
      chart: rook-ceph-cluster
      version: "v1.20.2"
      sourceRef:
        kind: HelmRepository
        name: rook-release
        namespace: rook-ceph
      interval: 1h
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: -1
  values:
    cephImage:
      tag: v21.1.0
    toolbox:
      enabled: true
    cephClusterSpec:
      cephConfig:
        mon:
          mon_cluster_log_level: "info"
      dashboard:
        enabled: true
        ssl: false # the Ingress terminates TLS; mgr's own cert is self-signed and browsers reject it
      placement:
        all:
          tolerations:
            - key: node-role.kubernetes.io/control-plane
              operator: Exists
              effect: NoSchedule
      storage:
        useAllNodes: true
        useAllDevices: false
        deviceFilter: "^nvme0n1"
